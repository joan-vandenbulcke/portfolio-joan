import './styles.scss';

const Loader = () => {

  return (
    <section class="loader-container">
      <h1 class="loader-title">
        <span>Hello, nice</span>
        <span>to see</span>
        <span>you here</span>
      </h1>
    </section>
  )
};

export default Loader;
